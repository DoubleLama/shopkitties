<h3>Résultat de recherche</h3>  

<div class="container mt-3 mb-3">
    <div class="row justify-content-between align-items-center mx-auto">
        <% @results.each do |item| %>
            <div class="col-lg-3 col-md-6 mb-2">
                <div class="card mx-auto">
                <%if item.cat_picture.attached?%>
                        <%= image_tag item.cat_picture, alt: 'cat_picture', class:"img-thumbnail", style:"height:200px; object-fit:cover;" %>
                    <%else%>
                        <p>=== Il n'y a pas encore d'image ===</p>
                    <%end%>
                <div class="card-body p-3">
                    <p class="card-title my-1"><b><%= item.title %></b></p>
                    <%= link_to "Voir plus", item_path(item.id), class: "btn btn-info btn-block" %>
                </div>
                </div>
            </div>
        <% end %>
    </div>
</div>
<h3>Continuer à parcourir les autres photos </h3>  

<br>
